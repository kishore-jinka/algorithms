Generate all subsets of a given set
Print all subsets of set with sum = k
Count number of subsets of a set whose sum = k
Generate all permutations of a given string
Generate all permutations of a repeating set of numbers
N Queens possible on NXN chess board
Print all binary strings of length n
Max Diameter of Binary Tree