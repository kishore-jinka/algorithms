Generate all subsets of a given set using recursion.
Print all subsets of S with sum = k
Count number of subsets whose sum = k
Generate all permutations of a given string
Generate all permutations of a repeating set of numbers
NQueens possible on NXN chess board

